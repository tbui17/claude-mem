{
  "description": "claude-mem Codex CLI hook integration",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "_HP=$(printenv PATH 2>/dev/null || true); if [ -z \"$_HP\" ] && [ -n \"${SHELL:-}\" ]; then _HP=$(\"$SHELL\" -lc 'printf %s \"$PATH\"' 2>/dev/null || true); fi; _HP=$(printf '%s' \"$_HP\" | tr ' ' ':'); export PATH=\"${_HP:+$_HP:}$PATH\";  _C=\"${CLAUDE_CONFIG_DIR:-$HOME/.claude}\"; _E=\"${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT:-}}\"; _P=$({ [ -n \"$_E\" ] && printf '%s\\n' \"$_E\"; ls -dt \"$_C/plugins/cache/thedotmack/claude-mem\"/[0-9]*/ 2>/dev/null; printf '%s\\n' \"$_C/plugins/marketplaces/thedotmack/plugin\"; } | while IFS= read -r _R; do _R=\"${_R%/}\"; [ -d \"$_R/plugin/scripts\" ] && _Q=\"$_R/plugin\" || _Q=\"$_R\"; [ -f \"$_Q/scripts/version-check.js\" ] && { printf '%s\\n' \"$_Q\"; break; }; done); [ -n \"$_P\" ] || { echo \"claude-mem: version-check.js not found\" >&2; exit 1; }; command -v cygpath >/dev/null 2>&1 && { _W=$(cygpath -w \"$_P\" 2>/dev/null); [ -n \"$_W\" ] && _P=\"$_W\"; }; CLAUDE_MEM_CODEX_HOOK=1 node \"$_P/scripts/version-check.js\"",
            "commandWindows": "$ErrorActionPreference = 'Stop'; $PSNativeCommandUseErrorActionPreference = $false; $claudeConfig = if ($env:CLAUDE_CONFIG_DIR) { $env:CLAUDE_CONFIG_DIR } else { Join-Path $HOME '.claude' }; $candidates = @(); if ($env:CLAUDE_PLUGIN_ROOT) { $candidates += $env:CLAUDE_PLUGIN_ROOT }; if ($env:PLUGIN_ROOT) { $candidates += $env:PLUGIN_ROOT }; $cacheRoot = Join-Path $claudeConfig 'plugins\\cache\\thedotmack\\claude-mem'; if (Test-Path $cacheRoot) { $candidates += Get-ChildItem -Path $cacheRoot -Directory | Sort-Object { try { [version]$_.Name } catch { [version]'0.0.0' } } -Descending | ForEach-Object { $_.FullName } }; $candidates += Join-Path $claudeConfig 'plugins\\marketplaces\\thedotmack\\plugin'; $pluginRoot = $null; foreach ($candidate in $candidates) { if (-not $candidate) { continue }; $root = if (Test-Path (Join-Path $candidate 'plugin\\scripts')) { Join-Path $candidate 'plugin' } else { $candidate }; if (Test-Path (Join-Path $root 'scripts\\version-check.js')) { $pluginRoot = $root; break } }; if (-not $pluginRoot) { Write-Error 'claude-mem: version-check.js not found'; exit 1 }; $versionCheck = Join-Path $pluginRoot 'scripts\\version-check.js'; $env:CLAUDE_MEM_CODEX_HOOK = '1'; & node $versionCheck; exit $LASTEXITCODE",
            "timeout": 5
          },
          {
            "type": "command",
            "command": "_HP=$(printenv PATH 2>/dev/null || true); if [ -z \"$_HP\" ] && [ -n \"${SHELL:-}\" ]; then _HP=$(\"$SHELL\" -lc 'printf %s \"$PATH\"' 2>/dev/null || true); fi; _HP=$(printf '%s' \"$_HP\" | tr ' ' ':'); export PATH=\"${_HP:+$_HP:}$PATH\";  _C=\"${CLAUDE_CONFIG_DIR:-$HOME/.claude}\"; _E=\"${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT:-}}\"; _P=$({ [ -n \"$_E\" ] && printf '%s\\n' \"$_E\"; ls -dt \"$_C/plugins/cache/thedotmack/claude-mem\"/[0-9]*/ 2>/dev/null; printf '%s\\n' \"$_C/plugins/marketplaces/thedotmack/plugin\"; } | while IFS= read -r _R; do _R=\"${_R%/}\"; [ -d \"$_R/plugin/scripts\" ] && _Q=\"$_R/plugin\" || _Q=\"$_R\"; [ -f \"$_Q/scripts/bun-runner.js\" ] && [ -f \"$_Q/scripts/worker-service.cjs\" ] && { printf '%s\\n' \"$_Q\"; break; }; done); [ -n \"$_P\" ] || { echo \"claude-mem: plugin scripts not found\" >&2; exit 1; }; command -v cygpath >/dev/null 2>&1 && { _W=$(cygpath -w \"$_P\" 2>/dev/null); [ -n \"$_W\" ] && _P=\"$_W\"; }; node \"$_P/scripts/bun-runner.js\" \"$_P/scripts/worker-service.cjs\" start >/dev/null",
            "commandWindows": "$ErrorActionPreference = 'Stop'; $PSNativeCommandUseErrorActionPreference = $false; $claudeConfig = if ($env:CLAUDE_CONFIG_DIR) { $env:CLAUDE_CONFIG_DIR } else { Join-Path $HOME '.claude' }; $candidates = @(); if ($env:CLAUDE_PLUGIN_ROOT) { $candidates += $env:CLAUDE_PLUGIN_ROOT }; if ($env:PLUGIN_ROOT) { $candidates += $env:PLUGIN_ROOT }; $cacheRoot = Join-Path $claudeConfig 'plugins\\cache\\thedotmack\\claude-mem'; if (Test-Path $cacheRoot) { $candidates += Get-ChildItem -Path $cacheRoot -Directory | Sort-Object { try { [version]$_.Name } catch { [version]'0.0.0' } } -Descending | ForEach-Object { $_.FullName } }; $candidates += Join-Path $claudeConfig 'plugins\\marketplaces\\thedotmack\\plugin'; $pluginRoot = $null; foreach ($candidate in $candidates) { if (-not $candidate) { continue }; $root = if (Test-Path (Join-Path $candidate 'plugin\\scripts')) { Join-Path $candidate 'plugin' } else { $candidate }; if ((Test-Path (Join-Path $root 'scripts\\bun-runner.js')) -and (Test-Path (Join-Path $root 'scripts\\worker-service.cjs'))) { $pluginRoot = $root; break } }; if (-not $pluginRoot) { Write-Error 'claude-mem: plugin scripts not found'; exit 1 }; $runner = Join-Path $pluginRoot 'scripts\\bun-runner.js'; $worker = Join-Path $pluginRoot 'scripts\\worker-service.cjs'; $startOutput = & node $runner $worker start; $startExit = $LASTEXITCODE; if ($startExit -ne 0) { if ($startOutput) { Write-Output $startOutput }; exit $startExit }; exit 0",
            "timeout": 60
          },
          {
            "type": "command",
            "command": "_HP=$(printenv PATH 2>/dev/null || true); if [ -z \"$_HP\" ] && [ -n \"${SHELL:-}\" ]; then _HP=$(\"$SHELL\" -lc 'printf %s \"$PATH\"' 2>/dev/null || true); fi; _HP=$(printf '%s' \"$_HP\" | tr ' ' ':'); export PATH=\"${_HP:+$_HP:}$PATH\";  _C=\"${CLAUDE_CONFIG_DIR:-$HOME/.claude}\"; _E=\"${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT:-}}\"; _P=$({ [ -n \"$_E\" ] && printf '%s\\n' \"$_E\"; ls -dt \"$_C/plugins/cache/thedotmack/claude-mem\"/[0-9]*/ 2>/dev/null; printf '%s\\n' \"$_C/plugins/marketplaces/thedotmack/plugin\"; } | while IFS= read -r _R; do _R=\"${_R%/}\"; [ -d \"$_R/plugin/scripts\" ] && _Q=\"$_R/plugin\" || _Q=\"$_R\"; [ -f \"$_Q/scripts/bun-runner.js\" ] && [ -f \"$_Q/scripts/worker-service.cjs\" ] && { printf '%s\\n' \"$_Q\"; break; }; done); [ -n \"$_P\" ] || { echo \"claude-mem: plugin scripts not found\" >&2; exit 1; }; command -v cygpath >/dev/null 2>&1 && { _W=$(cygpath -w \"$_P\" 2>/dev/null); [ -n \"$_W\" ] && _P=\"$_W\"; }; node \"$_P/scripts/bun-runner.js\" \"$_P/scripts/worker-service.cjs\" hook codex context",
            "commandWindows": "$ErrorActionPreference = 'Stop'; $PSNativeCommandUseErrorActionPreference = $false; $claudeConfig = if ($env:CLAUDE_CONFIG_DIR) { $env:CLAUDE_CONFIG_DIR } else { Join-Path $HOME '.claude' }; $candidates = @(); if ($env:CLAUDE_PLUGIN_ROOT) { $candidates += $env:CLAUDE_PLUGIN_ROOT }; if ($env:PLUGIN_ROOT) { $candidates += $env:PLUGIN_ROOT }; $cacheRoot = Join-Path $claudeConfig 'plugins\\cache\\thedotmack\\claude-mem'; if (Test-Path $cacheRoot) { $candidates += Get-ChildItem -Path $cacheRoot -Directory | Sort-Object { try { [version]$_.Name } catch { [version]'0.0.0' } } -Descending | ForEach-Object { $_.FullName } }; $candidates += Join-Path $claudeConfig 'plugins\\marketplaces\\thedotmack\\plugin'; $pluginRoot = $null; foreach ($candidate in $candidates) { if (-not $candidate) { continue }; $root = if (Test-Path (Join-Path $candidate 'plugin\\scripts')) { Join-Path $candidate 'plugin' } else { $candidate }; if ((Test-Path (Join-Path $root 'scripts\\bun-runner.js')) -and (Test-Path (Join-Path $root 'scripts\\worker-service.cjs'))) { $pluginRoot = $root; break } }; if (-not $pluginRoot) { Write-Error 'claude-mem: plugin scripts not found'; exit 1 }; $runner = Join-Path $pluginRoot 'scripts\\bun-runner.js'; $worker = Join-Path $pluginRoot 'scripts\\worker-service.cjs'; & node $runner $worker hook codex context; exit $LASTEXITCODE",
            "timeout": 60,
            "statusMessage": "Loading claude-mem context"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "_HP=$(printenv PATH 2>/dev/null || true); if [ -z \"$_HP\" ] && [ -n \"${SHELL:-}\" ]; then _HP=$(\"$SHELL\" -lc 'printf %s \"$PATH\"' 2>/dev/null || true); fi; _HP=$(printf '%s' \"$_HP\" | tr ' ' ':'); export PATH=\"${_HP:+$_HP:}$PATH\";  _C=\"${CLAUDE_CONFIG_DIR:-$HOME/.claude}\"; _E=\"${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT:-}}\"; _P=$({ [ -n \"$_E\" ] && printf '%s\\n' \"$_E\"; ls -dt \"$_C/plugins/cache/thedotmack/claude-mem\"/[0-9]*/ 2>/dev/null; printf '%s\\n' \"$_C/plugins/marketplaces/thedotmack/plugin\"; } | while IFS= read -r _R; do _R=\"${_R%/}\"; [ -d \"$_R/plugin/scripts\" ] && _Q=\"$_R/plugin\" || _Q=\"$_R\"; [ -f \"$_Q/scripts/bun-runner.js\" ] && [ -f \"$_Q/scripts/worker-service.cjs\" ] && { printf '%s\\n' \"$_Q\"; break; }; done); [ -n \"$_P\" ] || { echo \"claude-mem: plugin scripts not found\" >&2; exit 1; }; command -v cygpath >/dev/null 2>&1 && { _W=$(cygpath -w \"$_P\" 2>/dev/null); [ -n \"$_W\" ] && _P=\"$_W\"; }; node \"$_P/scripts/bun-runner.js\" \"$_P/scripts/worker-service.cjs\" hook codex session-init",
            "commandWindows": "$ErrorActionPreference = 'Stop'; $PSNativeCommandUseErrorActionPreference = $false; $claudeConfig = if ($env:CLAUDE_CONFIG_DIR) { $env:CLAUDE_CONFIG_DIR } else { Join-Path $HOME '.claude' }; $candidates = @(); if ($env:CLAUDE_PLUGIN_ROOT) { $candidates += $env:CLAUDE_PLUGIN_ROOT }; if ($env:PLUGIN_ROOT) { $candidates += $env:PLUGIN_ROOT }; $cacheRoot = Join-Path $claudeConfig 'plugins\\cache\\thedotmack\\claude-mem'; if (Test-Path $cacheRoot) { $candidates += Get-ChildItem -Path $cacheRoot -Directory | Sort-Object { try { [version]$_.Name } catch { [version]'0.0.0' } } -Descending | ForEach-Object { $_.FullName } }; $candidates += Join-Path $claudeConfig 'plugins\\marketplaces\\thedotmack\\plugin'; $pluginRoot = $null; foreach ($candidate in $candidates) { if (-not $candidate) { continue }; $root = if (Test-Path (Join-Path $candidate 'plugin\\scripts')) { Join-Path $candidate 'plugin' } else { $candidate }; if ((Test-Path (Join-Path $root 'scripts\\bun-runner.js')) -and (Test-Path (Join-Path $root 'scripts\\worker-service.cjs'))) { $pluginRoot = $root; break } }; if (-not $pluginRoot) { Write-Error 'claude-mem: plugin scripts not found'; exit 1 }; $runner = Join-Path $pluginRoot 'scripts\\bun-runner.js'; $worker = Join-Path $pluginRoot 'scripts\\worker-service.cjs'; & node $runner $worker hook codex session-init; exit $LASTEXITCODE",
            "timeout": 60
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "^Bash$|^mcp__.+__(read|view|cat)(_file|_files)?$",
        "hooks": [
          {
            "type": "command",
            "command": "_HP=$(printenv PATH 2>/dev/null || true); if [ -z \"$_HP\" ] && [ -n \"${SHELL:-}\" ]; then _HP=$(\"$SHELL\" -lc 'printf %s \"$PATH\"' 2>/dev/null || true); fi; _HP=$(printf '%s' \"$_HP\" | tr ' ' ':'); export PATH=\"${_HP:+$_HP:}$PATH\";  _C=\"${CLAUDE_CONFIG_DIR:-$HOME/.claude}\"; _E=\"${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT:-}}\"; _P=$({ [ -n \"$_E\" ] && printf '%s\\n' \"$_E\"; ls -dt \"$_C/plugins/cache/thedotmack/claude-mem\"/[0-9]*/ 2>/dev/null; printf '%s\\n' \"$_C/plugins/marketplaces/thedotmack/plugin\"; } | while IFS= read -r _R; do _R=\"${_R%/}\"; [ -d \"$_R/plugin/scripts\" ] && _Q=\"$_R/plugin\" || _Q=\"$_R\"; [ -f \"$_Q/scripts/bun-runner.js\" ] && [ -f \"$_Q/scripts/worker-service.cjs\" ] && { printf '%s\\n' \"$_Q\"; break; }; done); [ -n \"$_P\" ] || { echo \"claude-mem: plugin scripts not found\" >&2; exit 1; }; command -v cygpath >/dev/null 2>&1 && { _W=$(cygpath -w \"$_P\" 2>/dev/null); [ -n \"$_W\" ] && _P=\"$_W\"; }; node \"$_P/scripts/bun-runner.js\" \"$_P/scripts/worker-service.cjs\" hook codex file-context",
            "commandWindows": "$ErrorActionPreference = 'Stop'; $PSNativeCommandUseErrorActionPreference = $false; $claudeConfig = if ($env:CLAUDE_CONFIG_DIR) { $env:CLAUDE_CONFIG_DIR } else { Join-Path $HOME '.claude' }; $candidates = @(); if ($env:CLAUDE_PLUGIN_ROOT) { $candidates += $env:CLAUDE_PLUGIN_ROOT }; if ($env:PLUGIN_ROOT) { $candidates += $env:PLUGIN_ROOT }; $cacheRoot = Join-Path $claudeConfig 'plugins\\cache\\thedotmack\\claude-mem'; if (Test-Path $cacheRoot) { $candidates += Get-ChildItem -Path $cacheRoot -Directory | Sort-Object { try { [version]$_.Name } catch { [version]'0.0.0' } } -Descending | ForEach-Object { $_.FullName } }; $candidates += Join-Path $claudeConfig 'plugins\\marketplaces\\thedotmack\\plugin'; $pluginRoot = $null; foreach ($candidate in $candidates) { if (-not $candidate) { continue }; $root = if (Test-Path (Join-Path $candidate 'plugin\\scripts')) { Join-Path $candidate 'plugin' } else { $candidate }; if ((Test-Path (Join-Path $root 'scripts\\bun-runner.js')) -and (Test-Path (Join-Path $root 'scripts\\worker-service.cjs'))) { $pluginRoot = $root; break } }; if (-not $pluginRoot) { Write-Error 'claude-mem: plugin scripts not found'; exit 1 }; $runner = Join-Path $pluginRoot 'scripts\\bun-runner.js'; $worker = Join-Path $pluginRoot 'scripts\\worker-service.cjs'; & node $runner $worker hook codex file-context; exit $LASTEXITCODE",
            "timeout": 30
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "_HP=$(printenv PATH 2>/dev/null || true); if [ -z \"$_HP\" ] && [ -n \"${SHELL:-}\" ]; then _HP=$(\"$SHELL\" -lc 'printf %s \"$PATH\"' 2>/dev/null || true); fi; _HP=$(printf '%s' \"$_HP\" | tr ' ' ':'); export PATH=\"${_HP:+$_HP:}$PATH\";  _C=\"${CLAUDE_CONFIG_DIR:-$HOME/.claude}\"; _E=\"${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT:-}}\"; _P=$({ [ -n \"$_E\" ] && printf '%s\\n' \"$_E\"; ls -dt \"$_C/plugins/cache/thedotmack/claude-mem\"/[0-9]*/ 2>/dev/null; printf '%s\\n' \"$_C/plugins/marketplaces/thedotmack/plugin\"; } | while IFS= read -r _R; do _R=\"${_R%/}\"; [ -d \"$_R/plugin/scripts\" ] && _Q=\"$_R/plugin\" || _Q=\"$_R\"; [ -f \"$_Q/scripts/bun-runner.js\" ] && [ -f \"$_Q/scripts/worker-service.cjs\" ] && { printf '%s\\n' \"$_Q\"; break; }; done); [ -n \"$_P\" ] || { echo \"claude-mem: plugin scripts not found\" >&2; exit 1; }; command -v cygpath >/dev/null 2>&1 && { _W=$(cygpath -w \"$_P\" 2>/dev/null); [ -n \"$_W\" ] && _P=\"$_W\"; }; node \"$_P/scripts/bun-runner.js\" \"$_P/scripts/worker-service.cjs\" hook codex observation",
            "commandWindows": "$ErrorActionPreference = 'Stop'; $PSNativeCommandUseErrorActionPreference = $false; $claudeConfig = if ($env:CLAUDE_CONFIG_DIR) { $env:CLAUDE_CONFIG_DIR } else { Join-Path $HOME '.claude' }; $candidates = @(); if ($env:CLAUDE_PLUGIN_ROOT) { $candidates += $env:CLAUDE_PLUGIN_ROOT }; if ($env:PLUGIN_ROOT) { $candidates += $env:PLUGIN_ROOT }; $cacheRoot = Join-Path $claudeConfig 'plugins\\cache\\thedotmack\\claude-mem'; if (Test-Path $cacheRoot) { $candidates += Get-ChildItem -Path $cacheRoot -Directory | Sort-Object { try { [version]$_.Name } catch { [version]'0.0.0' } } -Descending | ForEach-Object { $_.FullName } }; $candidates += Join-Path $claudeConfig 'plugins\\marketplaces\\thedotmack\\plugin'; $pluginRoot = $null; foreach ($candidate in $candidates) { if (-not $candidate) { continue }; $root = if (Test-Path (Join-Path $candidate 'plugin\\scripts')) { Join-Path $candidate 'plugin' } else { $candidate }; if ((Test-Path (Join-Path $root 'scripts\\bun-runner.js')) -and (Test-Path (Join-Path $root 'scripts\\worker-service.cjs'))) { $pluginRoot = $root; break } }; if (-not $pluginRoot) { Write-Error 'claude-mem: plugin scripts not found'; exit 1 }; $runner = Join-Path $pluginRoot 'scripts\\bun-runner.js'; $worker = Join-Path $pluginRoot 'scripts\\worker-service.cjs'; & node $runner $worker hook codex observation; exit $LASTEXITCODE",
            "timeout": 120
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "_HP=$(printenv PATH 2>/dev/null || true); if [ -z \"$_HP\" ] && [ -n \"${SHELL:-}\" ]; then _HP=$(\"$SHELL\" -lc 'printf %s \"$PATH\"' 2>/dev/null || true); fi; _HP=$(printf '%s' \"$_HP\" | tr ' ' ':'); export PATH=\"${_HP:+$_HP:}$PATH\";  _C=\"${CLAUDE_CONFIG_DIR:-$HOME/.claude}\"; _E=\"${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT:-}}\"; _P=$({ [ -n \"$_E\" ] && printf '%s\\n' \"$_E\"; ls -dt \"$_C/plugins/cache/thedotmack/claude-mem\"/[0-9]*/ 2>/dev/null; printf '%s\\n' \"$_C/plugins/marketplaces/thedotmack/plugin\"; } | while IFS= read -r _R; do _R=\"${_R%/}\"; [ -d \"$_R/plugin/scripts\" ] && _Q=\"$_R/plugin\" || _Q=\"$_R\"; [ -f \"$_Q/scripts/bun-runner.js\" ] && [ -f \"$_Q/scripts/worker-service.cjs\" ] && { printf '%s\\n' \"$_Q\"; break; }; done); [ -n \"$_P\" ] || { echo \"claude-mem: plugin scripts not found\" >&2; exit 1; }; command -v cygpath >/dev/null 2>&1 && { _W=$(cygpath -w \"$_P\" 2>/dev/null); [ -n \"$_W\" ] && _P=\"$_W\"; }; node \"$_P/scripts/bun-runner.js\" \"$_P/scripts/worker-service.cjs\" hook codex summarize",
            "commandWindows": "$ErrorActionPreference = 'Stop'; $PSNativeCommandUseErrorActionPreference = $false; $claudeConfig = if ($env:CLAUDE_CONFIG_DIR) { $env:CLAUDE_CONFIG_DIR } else { Join-Path $HOME '.claude' }; $candidates = @(); if ($env:CLAUDE_PLUGIN_ROOT) { $candidates += $env:CLAUDE_PLUGIN_ROOT }; if ($env:PLUGIN_ROOT) { $candidates += $env:PLUGIN_ROOT }; $cacheRoot = Join-Path $claudeConfig 'plugins\\cache\\thedotmack\\claude-mem'; if (Test-Path $cacheRoot) { $candidates += Get-ChildItem -Path $cacheRoot -Directory | Sort-Object { try { [version]$_.Name } catch { [version]'0.0.0' } } -Descending | ForEach-Object { $_.FullName } }; $candidates += Join-Path $claudeConfig 'plugins\\marketplaces\\thedotmack\\plugin'; $pluginRoot = $null; foreach ($candidate in $candidates) { if (-not $candidate) { continue }; $root = if (Test-Path (Join-Path $candidate 'plugin\\scripts')) { Join-Path $candidate 'plugin' } else { $candidate }; if ((Test-Path (Join-Path $root 'scripts\\bun-runner.js')) -and (Test-Path (Join-Path $root 'scripts\\worker-service.cjs'))) { $pluginRoot = $root; break } }; if (-not $pluginRoot) { Write-Error 'claude-mem: plugin scripts not found'; exit 1 }; $runner = Join-Path $pluginRoot 'scripts\\bun-runner.js'; $worker = Join-Path $pluginRoot 'scripts\\worker-service.cjs'; & node $runner $worker hook codex summarize; exit $LASTEXITCODE",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
